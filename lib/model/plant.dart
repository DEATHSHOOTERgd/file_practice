class Plant{
   String common, botanical, zone, light, price, availability, imagen;

  Plant(this.common, this.botanical, this.zone, this.light, this.price, this.availability, this.imagen);

get Common => this.common;
get Botanical => this.botanical;
get Zone => this.zone;
get Light => this.light;
get Price => this.price;
get Availability => this.availability;
get Imagen => this.imagen;
}